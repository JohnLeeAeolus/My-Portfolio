[build]
  command = "npm run build"
  publish = "dist"

# React Router (SPA) redirect: allow direct loads on nested routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
